export default class WordList {
  name: string = ''
  fileName: string = ''

  constructor(
    name: string,
    fileName: string,
  ) {
    this.name = name
    this.fileName = fileName
  }
}